# Works fine even if we don't mention these because all the values are set to default
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    enabled: true